$('.post-comments-dash').css('display','block');
$('form.comment-form[id=<%= @comment.post.id %>]')[0].reset();
$('.post-comments[data-id=<%= @comment.post.id %>]').append("<%= escape_javascript render(partial: 'comment', locals:{comment: @comment}) %>");
